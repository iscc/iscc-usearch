# Tutorials

Step-by-step guides that walk you through using `iscc-usearch` from first install to working code.
Each tutorial builds on the previous one.

- **[Getting Started](getting-started.md)** -- Create your first index, add vectors, search, and
    persist results.
- **[Variable-Length Vectors](variable-length.md)** -- Mix vectors of different bit-lengths in the
    same index and understand prefix matching.
